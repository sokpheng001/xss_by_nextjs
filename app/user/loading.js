export default function Loading(props) {
  return (
    <main className="bg-gradient-to-b from-cyan-100 to-white gap-1 flex min-h-screen flex-col items-center justify-center">
        <p className="animate-pulse">Loading...</p>
    </main>
  );
}
